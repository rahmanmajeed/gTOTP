/**
 * @digest {options}
 */

export const digest = (options: any) => {
  
  //unpack options
  let secret = options.secret

};


/**
 * @generate {secret}
 * @params {options}
 * @return {secret}
 */

export const generateSecret = (options:any) => {
  
}